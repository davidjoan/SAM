<resources>

    <style name="SamTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ColorPrimary</item>
        <item name="colorPrimaryDark">@color/ColorPrimaryDark</item>
    </style>

    <style name="ButtonAppTheme" parent="android:Widget.Holo.Light.Button">
        <item name="android:background">@color/ColorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="SamTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="SamTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
